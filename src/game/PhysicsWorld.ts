export class PhysicsWorld {
  // Simple physics world - mainly for organization
  // Most physics is handled in Ball and Player classes
  
  public update(_delta: number): void {
    // Could add more complex physics here if needed
    // For now, physics is handled per-object
  }
}
